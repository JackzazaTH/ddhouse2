# BANDD789 — Vite + React (TS) (fixed)
- แก้บั๊ก `String.strip()` → ใช้ `trim()` + normalize slug แล้ว
- vercel.json แบบเรียบ ลด WARN (ใช้ routes rewrite)

## รัน
npm i
npm run dev

## Build/Deploy (Vercel)
Build Command: npm run build
Output: dist
